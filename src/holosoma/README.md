# HoloSoma training package

This package contains the PPO/DAgger training framework, Isaac Sim backend,
perception, managers and checkpoint/ONNX contracts used by this release.

Use the root-level `train_teacher.py` and `train_distillation.py` scripts.
See the [README](../../README.md) for installation and launch commands.
